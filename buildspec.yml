version: 0.1

phases:
    build:
        commands:
            - yarn install
            - yarn webpack
artifacts:
    files:
        - index.html
        - favicon.ico
        - styles/*
        - images/*
        - dist/bundle.js